# Production overrides for auth-service

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/farmstomarket_auth
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

auth-service:
  rate-limiting:
    login-attempts: 5
    window: 15m
